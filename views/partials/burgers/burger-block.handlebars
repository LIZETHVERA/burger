<li>
	{{burger_name}}

	<button class="devoured" data-id="{{id}}" data-newburger="{{devoured}}">
		{{#if devoured}}DEVOURED!{{else}}WAKE UP!{{/if}}
	</button>

	<button class="delete-cat" data-id="{{this.id}}">DELETE!</button>
</li>